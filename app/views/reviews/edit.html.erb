<div class="col-md-6 col-md-offset-3">
    
    <div class="edit-review-form">
<h1>Commentaar aanpassen</h1>

<%= simple_form_for([@good, @review]) do |f| %>
<%= f.input :comment %>
<%= f.button :submit, "Aanpassen",  :class => 'btn-custom2' %>

<% end %>
</div>